/** Set to true to re-enable WhatsApp integration across desktop, cloud-ai, and website. */
export const WHATSAPP_INTEGRATION_ENABLED = false;

/** Set to true to re-enable Meta integrations (Facebook, Instagram, Threads) across desktop, cloud-ai, and website. */
export const META_INTEGRATION_ENABLED = false;

/** Set to true to re-enable Outlook integration across desktop, cloud-ai, and website. */
export const OUTLOOK_INTEGRATION_ENABLED = false;

/** Set to true to re-enable Discord integration across desktop, cloud-ai, and website. */
export const DISCORD_INTEGRATION_ENABLED = false;

/** Set to true to re-enable Reddit integration across desktop, cloud-ai, and website. */
export const REDDIT_INTEGRATION_ENABLED = false;
