# Slim requirements for headless Linux VM (no GUI, no media capture, no desktop deps)
# Used by: stuard-python-agent.service on cloud VMs

# Core web framework (for WebSocket server)
websockets>=12.0
aiohttp>=3.9.0

# Env / Config
python-dotenv==1.0.0

# Filesystem, system, HTTP tools
aiofiles>=23.0.0

# Data processing
numpy>=1.26.0

# Database (local SQLite wrapper used by db_* tools)
# (stdlib sqlite3 is sufficient, no extra package needed)

# Encryption (for secure storage)
cryptography>=42.0.0

# Optional: rich logging
rich>=13.0.0
