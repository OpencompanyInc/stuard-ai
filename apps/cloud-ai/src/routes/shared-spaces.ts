/**
 * Shared Spaces API Routes
 * 
 * Endpoints for syncing local spaces to cloud and sharing with others.
 * All space content is E2E encrypted - server only stores encrypted blobs.
 * 
 * Refactored to ./shared-spaces/ directory
 */

export { handleSharedSpacesRoutes } from './shared-spaces/index';
